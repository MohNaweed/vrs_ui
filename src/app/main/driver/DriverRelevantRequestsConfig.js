import DriverRelevantRequests from './DriverRelevantRequests';

export const DriverRelevantRequestsConfig = {
    settings: {
        layout: {
            config: {}
        }
    },
    routes  : [
        {
            path     : '/driver_relevant_reqeusts',
            component: DriverRelevantRequests
        }
    ]
};